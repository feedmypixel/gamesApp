(function( angular ){
    'use strict';

    var gamesAppController = angular.module( 'gamesApp.controller' );

    var controller = {

        IndexController: [ '$scope', function( $scope ){

        } ]
    };

    gamesAppController.controller( controller );

}( angular ));